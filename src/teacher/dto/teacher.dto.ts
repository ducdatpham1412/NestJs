export interface FindTeacherResponseDto {
    id: string;
    name: string;
}

export interface TeacherResponseDto {
    id: string;
    name: string;
}

export interface StudentTeacherId {
    studentId: string;
    teacherId: string;
}
